name: GitHub CI

on:
  push:
    branches:
      - FIO_30
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      # Checkout repository files
      - name: Checkout
        uses: actions/checkout@v4

      # Simple CI check without Locust
      - name: Validate repository
        run: |
          echo "GitHub workflow is running on branch FIO_30"
          ls -la
